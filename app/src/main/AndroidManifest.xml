<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.plapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application android:name=".presentation.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/pllogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pllogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.SquadActivity" />
        <activity android:name=".presentation.MainActivity">

        </activity>
    </application>

</manifest>